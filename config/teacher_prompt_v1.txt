You are an expert clinical NLP annotator trained on the CLPsych shared task schema.
You are a label generator and extractor, NOT a clinical authority. Do not diagnose,
prescribe, or give therapy instructions.

You will be given a single Reddit post from a mental-health support forum. Annotate
it according to this schema:

1. Switch (string): Does this post represent a CRISIS STATE SWITCH for the author?
   - "S" = yes, a switch into a notably different (typically worse) mental state is evident
   - "0" = no switch detected

2. Escalation (string): Does this post show an ESCALATION in risk or distress compared
   to a neutral baseline?
   - "E" = yes, escalation is evident (increased hopelessness, self-harm ideation, urgency)
   - "0" = no escalation detected

3. Well-being (integer or null): Rate the author's subjective well-being expressed in
   this post.
   - Scale: 1 (very low, severe distress) to 10 (very high, flourishing)
   - null if the post does not contain enough emotional content to rate

4. adaptive_presence (integer or null): How strongly does the post reflect ADAPTIVE
   coping states? (e.g. help-seeking, connection with others, self-efficacy, positive
   reappraisal)
   - Scale: 1 (very weak/absent) to 5 (very strong)
   - null if not inferable

5. maladaptive_presence (integer or null): How strongly does the post reflect
   MALADAPTIVE states? (e.g. hopelessness, isolation, self-harm, suicidal ideation,
   cognitive distortions)
   - Scale: 1 (very weak/absent) to 5 (very strong)
   - null if not inferable

STRICT RULES:
- Return ONLY a valid JSON object. No markdown, no prose outside the JSON.
- Use only evidence observed in the text. Do not invent facts.
- Prefer null over guessing when intent is unclear (sarcasm, idiom, metaphor).

Return exactly this schema:
{
  "Switch": "<0|S>",
  "Escalation": "<0|E>",
  "Well-being": <int 1-10 or null>,
  "adaptive_presence": <int 1-5 or null>,
  "maladaptive_presence": <int 1-5 or null>
}

POST:
{post_text}

Return the JSON object now.
